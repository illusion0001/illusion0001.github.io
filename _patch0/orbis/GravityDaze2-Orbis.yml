- game: "Gravity Rush 2"
  app_ver: "01.11"
  patch_ver: "1.1"
  name: "60 FPS Unlock (WORK-IN-PROGRESS)"
  author: "illusion"
  note: "Help wanted! Check the rest of the game and add it to lookup table.\nVersion 1.1 since it is a rewrite now has deltatime but lacks level check."
  arch: orbis
  patch_list:
        # Sleep draw thread for constant 8.33ms
        - [ bytes, 0x0084ddb1, "9090" ] # NOP
        - [ bytes, 0x0084ddac, "bf8d200000" ] # MOV EDI,0x208d
        - [ bytes, 0x00846980, "be00000000" ] # MOV ESI,0x0
        - [ bytes32, 0x01802d90, 0x49742400 ] # 1000000.0f # to be div'd
        # Check if game is loadding
        - [ bytes, 0x0085e9cc, "e8cd43fa00" ] # CALL 0x01802d9e
        - [ bytes, 0x01802d9e, "803d9b866f0001" ]   # CMP byte ptr [0x01efb440],0x1 # is level loading?
        - [ bytes, 0x01802da5, "7412" ]             # JZ 0x01802db9
        # if != 1, write deltatime
        - [ bytes, 0x01802da7, "c5222a1d19587500" ] # VCVTSI2SS XMM11,XMM11,dword ptr [0x01f585c8]
        - [ bytes, 0x01802daf, "c5a25e0dd9ffffff" ] # VDIVSS XMM1,XMM11,dword ptr [0x01802d90]
        - [ bytes, 0x01802db7, "eb08" ]             # JMP 0x01802dc1
        # else
        - [ bytes, 0x01802db9, "c5fa100d63836f00" ] # VMOVSS XMM1,dword ptr [0x01efb124]
        - [ bytes, 0x01802dc1, "c3" ]               # RET
        # level load start
        - [ bytes, 0x013c8f2f, "e8479e4300" ]     # CALL 0x01802d7b
        - [ bytes, 0x013c8f34, "eb1b" ]           # JMP 0x013c8f51
        - [ bytes, 0x01802d7b, "c605be866f0001" ] # MOV byte ptr [0x01efb440],0x1
        - [ bytes, 0x01802d82, "c3" ]             # RET
          # level load done
        - [ bytes, 0x013c8f8e, "ebca" ]       # JMP 0x013c8f5a
        - [ bytes, 0x013c8f5a, "e9249e4300" ] # JMP 0x01802d83
        - [ bytes, 0x01802d83, "c605b6866f0002" ] # MOV byte ptr [0x01efb440],0x2
        - [ bytes, 0x01802d8a, "c3" ]             # RET
        # Currently known softlock level lookup table.
        # ep01_d (script failure)
        # ep02_a (igc deadlock)
        # ep03_* (script failure)
        # sm_08  (double speed timer makes it unplayable)
        # sm_09  (script failure)
        # removed check for _d and _com for ep01 because there are too many collision just to fix ep01_d
        # comic sections all have the same level name (ep01_com)
        # todo: playthrough the story episodes and add any sections that
        # softlock the game to this lookup table
